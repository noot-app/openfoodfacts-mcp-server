services:
  openfoodfacts-mcp-server:
    build: .
    ports:
      - "8080:8080"
    environment:
      - OPENFOODFACTS_MCP_TOKEN=your-secret-token
      - DATA_DIR=/data
      - REFRESH_INTERVAL_SECONDS=0
      - PORT=8080
    volumes:
      - ./data:/data
